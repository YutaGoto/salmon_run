linter:
  rubocop:
    gems:
      - rubocop-md
      - rubocop-performance
      - rubocop-rspec
      - rubocop-rails
      - rubocop-yayoi
    config: .rubocop.yml
  eslint:
    npm_install: "development"
  stylelint:
    npm_install: false
